auto-install-peers = true
# Expo/Metro resolve many transitive deps (reanimated worklets, nativewind
# runtime, expo-router internals) by name, which pnpm's isolated store hides.
# A hoisted node_modules is the canonical fix for Expo in a pnpm monorepo.
node-linker = hoisted
